<h1>New Memo</h1>

<%= form_for :memo, url: memos_path do |f| %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %></p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %></p>

  <p>
    <%= f.submit %></p>

<% end %>


<!-- This is going to allow us to go back to the index action -->
<%= form_for :memo, url: memos_path do |f| %>
<% end %>

<%= link_to 'BACK', memos_path %>
